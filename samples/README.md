Drop real-world files here for CLI testing:

- MedWay_Products_Template_v3.xlsx (good data)
- Items.xlsx (messy legacy export)
- Random CSV from live system

Run: `npm run parse-file ./samples/<filename>`